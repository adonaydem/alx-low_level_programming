#include "main.h"

/**
 * mul - Check description
 * @a: Input number
 * @b: Input number
 * Description - the function returns 1 if input is digit
 * Return: mul
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
